[module]
name = "test_service"

[dependencies]
test_lib = { name = "test_lib" }

[metadata]
type = "e2e-test"
language = "rust"
build_mode = "docker"
breakfast_i_ate_last_week = "eggs and bacon"

[file_paths]
include = [
  "src/**/*.py",
  "config/**/*.yaml"
]
exclude = [
  "tests/**/*.py",
  "docs/**/*.md"
]